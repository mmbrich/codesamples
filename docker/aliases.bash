MY_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source ${MY_DIR}/bin/functions.sh 

alias docker="${DOCKER}"
alias docker-compose="${COMPOSER}"
alias apt-gulp=aptGulp
alias apt-composer=aptComposer
alias apt-npm=aptNpm
alias apt-bower=aptBower
alias apt-artisan=aptArtisan
